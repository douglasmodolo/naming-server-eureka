server:
  port: 8761
spring:
  application:
    name: naming-server
  management:
    tracing:
      sampling:
        probability: 1.0 #100%
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false